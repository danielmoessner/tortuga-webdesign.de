{
  "name": "tortuga-webdesign.de",
  "version": "0.0.0",
  "private": true,
  "description": "The tortuga-webdesign.de website.",
  "author": "Daniel Mössner",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "check": "vue-tsc --noEmit",
    "cms": "npx netlify-cms-proxy-server",
    "update": "npx npm-check-updates -u --timeout 60000 && npm install"
  },
  "dependencies": {
    "@nuxt/content": "==3.11.*",
    "@nuxt/image": "==2.*",
    "@tailwindcss/forms": "==0.5.*",
    "@tailwindcss/typography": "==0.5.*",
    "@tailwindcss/vite": "==4.2.*",
    "better-sqlite3": "==12.*",
    "nuxt": "==4.3.*",
    "oxfmt": "==0.*",
    "oxlint": "==1.*",
    "prettier": "==3.8.*",
    "typescript": "==5.*",
    "vue-tsc": "==3.*"
  }
}
